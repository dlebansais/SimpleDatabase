namespace Database
{
    /// <summary>
    ///     Represents initial parameters of a request that only query values in one or more tables, and does not modify them.
    /// </summary>
    public interface IQueryContext : IContext
    {
    }
}
